import { initialState } from '../initialState.js';
  
function pcpReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state;
    }
  }
  
  export default pcpReducer;
