import { initialState } from '../initialState.js';
  
function nokReducer(state = initialState.nextOfKin, action) {
    switch (action.type) {
      
      default:
        return state;
    }
  }
  
  export default nokReducer;
